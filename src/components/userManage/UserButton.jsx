const UserButton = (props) => {
  return (
    <button
      //   onClick={() => props.deleteTask(props.id)}
      className="w-[67px] h-[30px] text-white bg-[#f01e1e] rounded-md"
    >
      Delete
    </button>
  );
};

export default UserButton;
